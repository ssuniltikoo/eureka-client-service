spring:
  application:
    name: eureka-client-demo
server:
  port: 8073
eurka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-Url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost